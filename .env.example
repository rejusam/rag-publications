# Copy to .env for local runs. .env is gitignored — never commit a real key.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
GROQ_FALLBACK_MODEL=openai/gpt-oss-20b
LLM_TIMEOUT_S=20
RATE_LIMIT_PER_MIN=10
# Comma-separated; defaults to the production site plus local dev origins.
# ALLOWED_ORIGINS=https://rejusamjohn.pages.dev,http://localhost:5500
