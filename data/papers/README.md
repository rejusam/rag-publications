# Papers

Place publication PDFs here before running ingestion. These are not committed to git (copyrighted material).

The ingestion pipeline (`python -m src.ingest`) reads all `*.pdf` files from this directory.
